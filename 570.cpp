#include <bits/stdc++.h>
using namespace std;

int aggressiveCows(vector<int> &stalls, int k)
{
    int n = stalls.size();

    int m = *min_element(stalls.begin(), stalls.end());
    int M = *max_element(stalls.begin(), stalls.end());

    int diff = M - n ;

    
}

int main()
{

    return 0;
}